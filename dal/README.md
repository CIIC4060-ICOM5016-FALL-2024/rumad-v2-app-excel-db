# Data Access Layer
Provides the interface between the business-logic 
layer and the underlying database.
In other words, hides 
the details of interfacing with the database.
Uses Object-Oriented models,
while the underlying database is a relational database.

## Data Access Object